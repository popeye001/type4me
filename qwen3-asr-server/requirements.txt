# MLX is installed separately by build.sh with JIT mode (MLX_METAL_JIT=ON)
# for backward compatibility across macOS 14+. Do NOT pip install mlx from
# this file when building the distributable binary.  For local development,
# plain `pip install mlx` is fine.
mlx>=0.22.0
mlx-qwen3-asr>=0.1.0
fastapi>=0.115.0
uvicorn>=0.34.0
websockets>=14.0
soundfile>=0.12.0
numpy>=1.26.0
llama-cpp-python>=0.3.0
